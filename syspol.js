const SRC_LIB_PREFIX = './src/lib';

exports.app = require(SRC_LIB_PREFIX + '/app');
exports.fs = require(SRC_LIB_PREFIX + '/fs');
exports.path = require(SRC_LIB_PREFIX + '/path');
exports.util = require(SRC_LIB_PREFIX + '/util');
